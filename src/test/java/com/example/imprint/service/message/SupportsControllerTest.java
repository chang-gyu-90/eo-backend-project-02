package com.example.imprint.service.message;

import com.example.imprint.domain.message.SupportsController;
import com.example.imprint.domain.message.SupportsService;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.anyLong;
import static org.mockito.Mockito.*;

/**
 * SupportsController 테스트 - 최종본
 */
@ExtendWith(MockitoExtension.class)
@DisplayName("SupportsController 테스트")
class SupportsControllerTest {}
